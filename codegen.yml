overwrite: true
schema: "http://localhost:1337/graphql"
# documents: "src/**/*.graphql"
generates:
  ./types/models.d.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
  # ./graphql.schema.json:
  #   plugins:
  #     - "introspection"
